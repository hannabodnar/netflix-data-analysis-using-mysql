CREATE TABLE netflix_titles (

	title VARCHAR(255) PRIMARY KEY,
	type VARCHAR(255),
	country VARCHAR(255),
	date_added DATE,
	release_year DATE,
	rating VARCHAR(255),
	duration VARCHAR(255),
	listed_in VARCHAR(255)

)